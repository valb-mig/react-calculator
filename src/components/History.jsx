import React from 'react';
import './scss/history.scss'

const History = (props) => {
    return(
        <div className='history'>
            {props.title}
        </div>
    );
}
export default History;